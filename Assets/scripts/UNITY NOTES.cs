using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UNITYNOTES : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}


// kinematic rigidbodys do not react to physics forces
// kinematic rigidbodys can be animated and moved by their transform


//colliders
// static colliders cant move and are good to use for things like walls and the ground
// dynamic colliders can move and have rigidbody attached